namespace KREAN.Core;

public static class Units
{
    /// <summary>1 Quake/TrenchBroom unit = 1/32 metre (a 56-unit player is 1.75 m).</summary>
    public const float QuakeToMeters = 1f / 32f;

    public const float Deg2Rad = MathF.PI / 180f;
}
